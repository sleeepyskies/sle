#pragma once

/**
 * @brief Used to represent the different mouse buttons. SDL uses macros for this,
 * such that for clarity, a custom enum has been defined here for typing parameters.
 */
enum MouseKey { LEFT_KEY, RIGHT_KEY, MIDDLE_KEY, FORWARD_KEY, BACKWARD_KEY };

